services:
  - type: web
    name: dinosaur-attack
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn config.wsgi:application -c gunicorn.conf.py"
    healthCheckPath: /
